spring:
  application:
    name: employee-service
  profiles:
    active:
    - dev
  cloud:
    config:
      discovery:
        enabled: true
      username: config-server
      password: config-server

encrypt:
  key: ABCDEFGHIJKLMNOPQRSTUVWXYZ